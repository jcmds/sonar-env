    mkdir -p /var/sonarqube/config && chown -R 1000:1000  /var/sonarqube/config && \
    mkdir -p /var/sonarqube/data  && chown -R 1000:1000  /var/sonarqube/data && \
    mkdir -p /var/sonarqube/log  && chown -R 1000:1000  /var/sonarqube/log && \
    mkdir -p /var/sonarqube/extension  && chown -R 1000:1000  /var/sonarqube/extension